#pragma once

// Экран ИК-термометра MLX90614 (модуль BCC6811002), I2C-адрес 0x5A.
//   - при входе проверяется наличие датчика на шине;
//   - выводит температуру объекта и окружения в °C;
//   - ↑/↓  — калибровочное смещение (°C), сохраняется в config.json;
//   - ←/→  — пресет поверхности (эмиссивность), коррекция программная.
//
// ВАЖНО: OLED работает на I2C 800 кГц, а MLX90614 (SMBus) — максимум 100 кГц,
// поэтому на время обращения к датчику тактовая частота шины снижается.
void tempLay();
